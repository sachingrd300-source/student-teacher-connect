rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function userRole() {
        return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role;
    }

    function isAdmin() {
        return isSignedIn() && userRole() == 'admin';
    }

    function isStudent() {
        return isSignedIn() && userRole() == 'student';
    }

    function isTutor() {
        return isSignedIn() && userRole() == 'tutor';
    }
    
    match /users/{userId} {
      allow get: if isSignedIn();
      allow list: if isSignedIn();
      // Allow self-creation OR a tutor creating a student profile
      allow create: if (request.auth.uid == userId) || 
                      (isTutor() && request.resource.data.role == 'student');
      allow update: if isOwner(userId) || isAdmin();
      allow delete: if false; // Keep delete restricted for now to prevent orphaned data
    }

    match /publicTutors/{tutorId} {
      allow get, list: if true;
      allow create: if isAdmin(); // Only admins can create a public profile (on approval)
      allow update: if isAdmin() || isOwner(tutorId); // Admin or owner can update
      allow delete: if isAdmin(); // Only admins can delete a public profile
    }

    match /classes/{classId} {
      allow create: if isTutor() && request.resource.data.teacherId == request.auth.uid;
      allow get, list: if isSignedIn();
      allow update, delete: if isTutor() && resource.data.teacherId == request.auth.uid;
    }

    match /enrollments/{enrollmentId} {
      // Allow teacher to create an 'approved' enrollment, or a student to create a 'pending' one
      allow create: if (isTutor() && request.resource.data.teacherId == request.auth.uid && request.resource.data.status == 'approved') ||
                      (isStudent() && request.resource.data.studentId == request.auth.uid && request.resource.data.status == 'pending');
      
      allow get: if isOwner(resource.data.studentId) || isOwner(resource.data.teacherId);
      
      // Allow list if the query is properly constrained by the calling code.
      // The `get` rule will be checked for each document.
      allow list: if isSignedIn();
      
      allow update: if isTutor() && resource.data.teacherId == request.auth.uid;
      
      allow delete: if (isTutor() && resource.data.teacherId == request.auth.uid) || isOwner(resource.data.studentId);
    }

    match /studyMaterials/{materialId} {
      allow create: if isTutor() && request.resource.data.teacherId == request.auth.uid;
      allow get, list: if true; // All materials are public
      allow update, delete: if isTutor() && resource.data.teacherId == request.auth.uid;
    }

    match /tests/{testId} {
      allow create: if isTutor() && request.resource.data.teacherId == request.auth.uid;
      allow get, list: if isSignedIn();
      allow update, delete: if isTutor() && resource.data.teacherId == request.auth.uid;
    }

    match /testResults/{resultId} {
      allow create: if isStudent() && request.resource.data.studentId == request.auth.uid;
      allow get: if isOwner(resource.data.studentId) || isOwner(resource.data.teacherId);
      // Allow list if the query is properly constrained by the calling code.
      // The `get` rule will be checked for each document.
      allow list: if isSignedIn();
      allow update, delete: if false;
    }

    match /attendance/{attendanceId} {
      allow create, update: if isTutor() && request.resource.data.teacherId == request.auth.uid;
      allow get, list: if isSignedIn();
      allow delete: if isTutor() && resource.data.teacherId == request.auth.uid;
    }

    match /announcements/{announcementId} {
      allow create: if isTutor() &&
                      request.resource.data.teacherId == request.auth.uid &&
                      get(/databases/$(database)/documents/classes/$(request.resource.data.classId)).data.teacherId == request.auth.uid;

      allow get, list: if true;

      allow delete: if isTutor() && resource.data.teacherId == request.auth.uid;
      allow update: if false;
    }
  }
}
