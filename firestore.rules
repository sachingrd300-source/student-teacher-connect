
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isUser() {
        return isSignedIn() && exists(/databases/$(database)/documents/users/$(request.auth.uid));
    }

    function userRole() {
        return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role;
    }

    function isAdmin() {
        return isUser() && userRole() == 'admin';
    }

    function isStudent() {
        return isUser() && userRole() == 'student';
    }

    function isTutor() {
        return isUser() && userRole() == 'tutor';
    }
    
    match /users/{userId} {
      allow get: if isOwner(userId) || isAdmin();
      allow list: if isAdmin();
      // Allow user creation for anyone signing up
      allow create: if true;
      allow update: if isOwner(userId) || isAdmin();
      allow delete: if false;
    }

    match /classes/{classId} {
      allow create: if isTutor() && request.resource.data.teacherId == request.auth.uid;
      allow get: if (isTutor() && resource.data.teacherId == request.auth.uid) || isStudent();
      allow list: if isTutor() || isStudent();
      allow update, delete: if isTutor() && resource.data.teacherId == request.auth.uid;
    }

    match /enrollments/{enrollmentId} {
      allow create: if isStudent()
                    && request.resource.data.studentId == request.auth.uid
                    && request.resource.data.status == 'pending'
                    && request.resource.data.teacherId != request.auth.uid;
      
      allow get: if (isStudent() && resource.data.studentId == request.auth.uid)
                 || (isTutor() && resource.data.teacherId == request.auth.uid);

      allow list: if isStudent() || isTutor();

      allow update: if isTutor()
                    && resource.data.teacherId == request.auth.uid
                    && resource.data.status == 'pending'
                    && request.resource.data.status in ['approved', 'denied']
                    && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['status']);
      
      allow delete: if false;
    }

    // Rules for pending students collection
    match /pendingStudents/{studentId} {
      // Allow anyone to read a pending student doc (for ID verification on signup)
      allow get: if true;

      // Allow tutors to create pending students
      allow create: if isTutor() && request.resource.data.teacherId == request.auth.uid;

      // Allow tutors to see the list of students they've created
      allow list: if isTutor(); 
      
      // A user can delete a pending doc once they sign up
      // Or the teacher who created it can delete it.
      allow delete: if isSignedIn() || (isTutor() && get(/databases/$(database)/documents/pendingStudents/$(studentId)).data.teacherId == request.auth.uid);
    }
  }
}
